{"id":"digits_sum_001","split":"test-similar","instruction":"Fix the Python function so it passes the tests. Return only code.","buggy_code":"def digit_sum(n):\n    total = 0\n    for ch in str(n):\n        total += int(ch)\n    return total","public_tests":["assert digit_sum(123) == 6","assert digit_sum(0) == 0"],"hidden_tests":["assert digit_sum(-45) == 9","assert digit_sum(10001) == 2"],"bug_type":"boundary_negative_number"}
{"id":"dedupe_001","split":"test-hard","instruction":"Fix the Python function so it preserves first occurrence order. Return only code.","buggy_code":"def unique_items(items):\n    return list(set(items))","public_tests":["assert unique_items([1, 2, 1, 3]) == [1, 2, 3]"],"hidden_tests":["assert unique_items(['b', 'a', 'b']) == ['b', 'a']","assert unique_items([]) == []"],"bug_type":"state_update_order"}
{"id":"clamp_001","split":"test-transfer","instruction":"Fix the Python function so it applies both bounds correctly. Return only code.","buggy_code":"def clamp(x, lo, hi):\n    if x < lo:\n        return lo\n    return x","public_tests":["assert clamp(2, 0, 5) == 2","assert clamp(-1, 0, 5) == 0"],"hidden_tests":["assert clamp(9, 0, 5) == 5","assert clamp(5, 0, 5) == 5"],"bug_type":"constraint_propagation"}

